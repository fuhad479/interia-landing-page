import { Kanit, Nunito } from 'next/font/google';

export const kanit = Kanit({ subsets: ['latin'], weight: ["400"], variable: '--font-kanit' });
export const nunito = Nunito({ subsets: ['latin'], weight: ["400"], variable: '--font-nunito' });
